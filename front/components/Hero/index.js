import classes from "@styles/components/hero.module.scss";

function index() {
  return (
    <section className={classes.hero}>
      <h1>Find out the latest chinchilla news</h1>
    </section>
  );
}

export default index;
